import { GroupItem } from '../../../../source';
import { Aliases } from './Aliases';
import { Triggers } from './Triggers';

export const Butterflybomb = new GroupItem(
    'Butterflybomb',
    [
        Aliases,
        Triggers
    ]
);
